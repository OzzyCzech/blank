{
	"name": "Blank",
	"description": "",
	"version": "0.0.0",

	"require-all": true,

	"minimum-stability": "dev",

	"require": {
		"php": ">=5.4.0",
		"om/wplugins": "*"
	},

	"scripts": {
		"post-update-cmd": ["echo [WARNING] Delete .git dirs", "rm -rf libs/**/**/.git"],
		"post-install-cmd": ["echo [WARNING] Delete .git dirs", "rm -rf libs/**/**/.git"]
	},

	"autoload": {
		"classmap": ["classes"]
	}
}
